services:
  # Backend API + WebSocket signaling server.
  # Free-tier topology: this single Render free instance pairs with the
  # frontend on Vercel, Postgres on Neon/Supabase (free), and Upstash Redis
  # REST (free). No TCP Redis / BullMQ needed — see docs/FREE_TIER_DEPLOY.md.
  - type: web
    name: webrtc-backend
    runtime: docker
    plan: free
    dockerfilePath: backend/Dockerfile
    dockerContext: backend
    healthCheckPath: /health
    autoDeploy: true
    # Run Drizzle migrations before each deploy finishes.
    preDeployCommand: bun run db:migrate
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 4000
      - key: TRUST_PROXY
        value: 1
      - key: DATABASE_POOL_MAX
        value: 5
      # ── Required: set in the Render dashboard (sync: false) ──
      - key: DATABASE_URL
        sync: false
      - key: ALLOWED_ORIGINS
        sync: false
      - key: UPSTASH_REDIS_REST_URL
        sync: false
      - key: UPSTASH_REDIS_REST_TOKEN
        sync: false
      - key: APP_URL
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_REFRESH_SECRET
        generateValue: true
      # Room-token signing key (WS upgrade + captions upload). Auto-generated
      # so prod uses a secret distinct from JWT_SECRET; code falls back to
      # JWT_SECRET when unset (local dev).
      - key: JWT_ROOM_SECRET
        generateValue: true
      # Mail via the Resend API — required for signup OTP and password
      # resets (fails fast when missing). Set values in the dashboard;
      # see docs/FREE_TIER_DEPLOY.md § Email (Resend). Never commit the key.
      - key: RESEND_API_KEY
        sync: false
      - key: EMAIL_FROM
        sync: false
      # ── Optional (free-tier integrations) ──
      - key: GOOGLE_CLIENT_ID
        sync: false
      - key: GOOGLE_CLIENT_SECRET
        sync: false
      - key: GOOGLE_CALLBACK_URL
        sync: false
      - key: DEEPGRAM_API_KEY
        sync: false
      - key: MAX_PARTICIPANTS
        value: 8
      # ── Scale-up only (paid): TCP Redis for BullMQ delayed/queued jobs.
      # Leave unset on the free tier — jobs run in-process instead.
      - key: REDIS_URL
        sync: false
